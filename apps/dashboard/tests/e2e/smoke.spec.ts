import { test, expect } from '@playwright/test';

// Smoke placeholder — real assertions land in T04/T05/T06 tickets.
test.skip('placeholder smoke test', () => {
  // No-op: ensures playwright --list exits 0 (at least 1 test file present).
  expect(true).toBe(true);
});
